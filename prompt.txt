# [Goal Description]

Polish three existing features to make them production-ready for v1:

1. **OA pages** — handle empty/coming-soon state gracefully since there are no real problems yet
2. **Settings page** — clean up UI, improve the Codeforces connection flow
3. **Admin OA management** — strengthen seed script with more companies/sections/problems

## Proposed Changes

### Part 1: OA pages — Coming Soon state (`src/app/(public)/OA/`)

**1a: `/OA/page.tsx`** — The empty state (lines 30-39) already looks decent. If `companiesList` is empty (no seeded data), the page shows a clean "coming soon" state. Keep as-is.

**1b: `/OA/[company]/page.tsx`** — The company roadmap page. If a company has 0 sections, `CompanyRoadmap` component should show an empty state instead of a blank page. Add check after `getCompanyBySlug()`:

If `sections.length === 0`, show a centered "Roadmap being curated" message instead of rendering `CompanyRoadmap`.

**1c: `/OA/[company]/[section]/page.tsx`** — Already has a "Content coming soon" empty state for no problems (lines 118-129). It's fine.

Update the empty state copy slightly to make it feel intentional:
- Title: "Roadmap in progress"
- Description: "We are curating high-quality OA problems for this section. Check back soon!"

---

### Part 2: Settings page polish (`src/app/(app)/settings/page.tsx` + `src/components/dashboard/platform-connections.tsx`)

**2a: Settings page layout**

Keep the current structure but improve spacing and visual hierarchy:
- Remove the uppercase "SETTINGS" label (line 23-25) — redundant, "Account settings" is clear enough
- Change heading to: "Settings" (clean, no "Account settings")
- Subtitle: "Manage your platform connections and account preferences."
- Add a divider below the heading
- The `PlatformConnections` card should have max-w-md to feel more focused

**2b: PlatformConnections component**

Current issues:
- Success/error messages use emerald/rose colors — keep minimal palette (no green/rose)
- The "Connected as" handle shows in `text-emerald-400` — change to `text-accent`
- The Disconnect button is `text-rose-500` — change to `text-slate-400 hover:text-white`
- The Connect button should use the app's accent color consistently

Replace emerald/rose colors:
- Error banner: `border-rose-500/20 bg-rose-500/10 text-rose-500` → `border-white/10 bg-white/5 text-slate-400`
- Success banner: `border-emerald-500/20 bg-emerald-500/10 text-emerald-500` → `border-white/10 bg-white/5 text-accent`
- Connected handle: `text-emerald-400` → `text-accent`
- Disconnect button: `text-rose-500` → `text-slate-400 hover:text-white`

---

### Part 3: Strengthen seed script (`scripts/seed-oa.ts`)

Add more companies, sections, and problems. Current seed has 3 companies, 2 sections each, 2 problems each. Expand to:

**Companies (keep existing + add):**
- Google (Hard) — existing
- Meta (Medium) — existing
- Amazon (Medium) — existing
- **Microsoft (Medium)** — new: arrays, trees, design
- **Netflix (Hard)** — new: system design, concurrency
- **Apple (Medium)** — new: strings, DP, OS concepts

**Sections per company** — increase from 2 to 3:

Google:
1. Graphs & Trees
2. Dynamic Programming
3. **Math & Logic** (new — 2 problems)

Meta:
1. Arrays & Strings
2. Binary Search & Window
3. **Recursion & Backtracking** (new — 2 problems)

Amazon:
1. Heaps & Priority Queues
2. Linked Lists & Stacks
3. **Greedy & Intervals** (new — 2 problems)

Microsoft:
1. Arrays & Sorting
2. Trees & BST
3. Object-Oriented Design

Netflix:
1. System Design Basics
2. Concurrency & Threading

Apple:
1. String Manipulation
2. Dynamic Programming

**Problems** — add 2 realistic LeetCode problems per new section. Each with real LeetCode URL, correct difficulty, and appropriate title.

The script should be idempotent (`ON CONFLICT` upserts).

---

### Verification

1. Run `npm run db:seed` (or the seed script directly) — verify no errors
2. Run `npm run typecheck` — no errors
3. Navigate `/OA` — shows company grid with 6 companies
4. Navigate `/OA/google` — shows Google sections
5. Navigate `/OA/google/graphs-and-trees` — shows problems
6. Admin: `/admin/oa` — all companies, sections, problems visible
7. `/settings` — clean layout, CF connect/disconnect works, no green/rose colors